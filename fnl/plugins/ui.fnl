(add "jake-stewart/auto-cmdheight.nvim")
(setup "auto-cmdheight")

(setup "mini.icons")
(setup "util.statusline")

; (let [notify (require :mini.notify)]
;   (notify.setup {:window {:config {:title ""}
;                           :winblend 0}})
;   (set vim.notify (notify.make_notify)))
